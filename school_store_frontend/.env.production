# Production environment variables for Render deployment
# The API will be served from the same domain, so we use relative paths
# VITE_API_BASE_URL is intentionally not set so the app uses relative paths